services:
  redis:
    image: redis:8.0-alpine
    container_name: pulsevote-redis
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    ports:
      - "6379:6379"

